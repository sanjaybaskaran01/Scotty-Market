/**
 * Dev feature flags. Flip these to skip onboarding / tutorial during development.
 * These are compile-time constants — no .env parsing needed.
 */

export const DEV_SKIP_ONBOARDING = false;
export const DEV_SKIP_TUTORIAL = false;
